[project]
name = "neo4j-app"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.139.0",
    "neo4j>=5.0",
    "uvicorn[standard]>=0.49.0",
]
[tool.pyright]
venvPath = "."
venv = ".venv"
